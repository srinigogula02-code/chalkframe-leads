# Chalkframe Leads

Private lead-research workspace for the Chalkframe team.

## Local development

Copy `.env.example` to `.env.local`, configure the Neon database connection and a long random session secret, then run:

```bash
npm install
npm run dev
```

The application blocks search indexing and requires an administrator-created account for access.
